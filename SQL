1.where 用法
  直接连立两表，不需要join
  本质上做了一个cross join
  记得group by 确保每个unique值返回

2.找重复的
  先找distinct list，再找id不在这个list里的删除
  
3.if语句
  when case... then...
       case... then...
  end
  
  也可以用if
  if（a>b, res1, res2）
  
套路总结：
1.按照colum找最值
  先按照colum group by找到最值，
  再从多个表选取值，用where连立选出最值
